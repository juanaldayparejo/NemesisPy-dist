from setuptools import setup

setup(name='NemesisPy',
      version='0.0',
      description='NEMESIS radiative transfer code',
      packages=['NemesisPy'],
      install_requires=['numpy','matplotlib','sympy','miepython'])
